# PRISM 2 — User-Impact Ledger

**What this is:** the journey-state tracker. WORKSTREAMS.md tracks *stream* state and PENDING.md
tracks *git/DB* state; this file tracks **who has to do something differently** after a change
lands — so user instructions, configuration guides, and training material can be produced from it
per role instead of reconstructed from memory at release time.

> Absolute path: `C:\Users\eugen\prism\docs\USER-IMPACT.md`
> Created 2026-07-28 on Eugene's direction (proposed by #8).

## Protocol

**If your change alters what any user sees, enters, or configures — add a row here in the same
commit.** Written by the stream that lands the change (you understand it best, freshest). Keep
rows terse; link the spec/PR. Instruction writing happens later, per role, from this ledger —
the row is the obligation, not the manual.

**Audit:** #15 (pendings tracker) reconciles this ledger against merged journey-affecting changes
on each refresh — a landed change with no row is a gap to flag. **Instruction-writing owner:
#11 (UI) — assigned by Eugene 2026-07-28.** #11 turns ledger rows into role-based user guides /
help text as the relevant surfaces stabilize; row authors remain responsible for row accuracy.

**Roles vocabulary** (from the tiered-access spec + board): `DAO` data-entry officer · `BLO`
Utility Liaison · `org-admin` utility org admin · `BMO` benchmarking-mgmt admin (PPA) · `PPA_FIN`
finance role · `DEV` developer/ops · `subscriber` consumer-tier user · `registrant` prospective
user.

**Status legend:** ⬜ instructions not written · 📝 drafted · ✅ published · 📣 comms sent ·
🕐 change not yet built (row is a forward obligation)

---

## Ledger

| # | Change (ref) | Landed? | Roles | Journey delta (before → after) | Instructions / config needed | Status |
|---|---|---|---|---|---|---|
| 1 | Admin MFA — TOTP enrolment + per-session challenge (#12, applied to dev DB 2026-07-26) | ✅ dev | BMO, DEV | Log in with magic link only → must enrol an authenticator app once, then pass a TOTP challenge each session; unverified admins get 401 on admin APIs | Enrolment walkthrough (authenticator app + **manual secret-key entry — no QR code**); recovery = the **10 backup codes** shown once at enrolment; **⚠ recovery gap: if device AND backup codes are BOTH lost there is NO self-service reset — a DEV must clear the user's `two_factor` row + set `two_factor_enabled=false` in the DB** (candidate for a future "reset 2FA" admin action); note the full-screen `/two-factor` gate; **PROD prerequisite:** run `scripts/sql/2026-07-26-admin-mfa.sql` before deploy | ⬜ |
| 2 | Sector terminology label layer — electricity `service_area` renders as **"Grid"** (PR #58) | ✅ | all UI users | Screens said "Service Area" → now say "Grid" (display-only; data and URLs unchanged) | One glossary note ("Grid = service area, electricity sector"); BMO heads-up that labels are data-editable (Grid→Network etc.) | ⬜ |
| 3 | Sentinel entities deleted — "All Utilities" org, "All Countries", "All Service Areas", All/Others sub-regions (one-shot, 2026-07-27) | ✅ dev DB | BMO, analysts | Pickers offered "All …" pseudo-entries; cross-utility numbers could sit under "All Utilities" → pickers list only real entities; "all X" views come from benchmarking/rollup screens, never a selectable fake entity | Note in admin guide: aggregates are computed views; if an old workflow "entered data against All Utilities", its replacement is the per-utility entry + automatic rollup | ⬜ |
| 4 | API key tiering — `API_KEY_SENSITIVE` for users/pbiRls/AzureToken endpoints (PR #73) | ✅ | DEV | One `API_KEY` for all service endpoints → sensitive endpoints prefer `API_KEY_SENSITIVE` (falls back to `API_KEY`, zero breakage) | DEV/operator-only — **no end-user impact.** Env guide: `API_KEY` still guards the bulk Power BI `dim*`/`fact*` ingestion (unchanged); `API_KEY_SENSITIVE` (optional) guards **exactly 3** endpoints — `/api/users`, `/api/pbiRls`, `/api/getAzureAccessToken`. To activate the split: set `API_KEY_SENSITIVE`, **repoint those 3 endpoints' Power BI data sources to send it**, then rotate `API_KEY` independently. Unset ⇒ falls back to `API_KEY` | ⬜ |
| 5 | Registration intake quiz + org picker (#10 spec §5.4, designed) | 🕐 not built | registrant, org-admin, BMO | Free-text "who are you / why" + free-text org name → structured purpose/engagement quiz + pick-or-propose org from live list (M49 country picker on propose-new) | Registrant-facing help text; BMO triage guide (structured requests, dedup candidates, routing); org-admin approve/decline flow | 🕐 |
| 6 | Grain-based data entry — screens target the measure's declared strata; virtual generators retired (#8/#11 pass, gated on #2 DDL) | 🕐 not built | DAO, BLO | Station/org/country data entered against "virtual generator" rows → each measure's screen targets its real level (unit / station / grid / organisation / country); virtual rows gone from pickers | Data-collector guide rewrite (the big one): where each measure now lives, what the level means, why virtuals disappeared; per-utility onboarding note when virtuals are retired | 🕐 |
| 7 | Unit lifecycle stints — capacity/location as state changes, not per-period entry (`unit-lifecycle-spec.md` draft; awaiting Eugene's 2 confirms) | 🕐 spec draft | DAO, BLO, BMO | Type rated capacity per unit per period; moves/derates invisible → record a stint event (move / derate / uprate / deactivate / reactivate) when it happens; capacity KPIs computed from stint history | How to record each event type incl. the derate-after-repair case; what seed stints mean for history; what the entry screen no longer asks; BMO unit-config guide | 🕐 |
| 8 | BSC Builder "+ Add KPI" → cascading Input/KPI picker modal (#5, PR #35) | ✅ dev | BLO | Build mode: "+ Add KPI" was one searchable KPI dropdown → now opens a modal: pick **Input or KPI**, then Category → Subcategory → metric, then Select; a **"Create New KPI"** button opens a classifier modal (submit still scaffolded). BLOs can now track **Inputs** on an initiative, not just KPIs; **context-fed measures (the 16 Country Context) are excluded** from the Input list (fed from country_context, not data-entry) | BLO Build-mode guide: the Input-vs-KPI choice, the cascade, and that "Create New" doesn't submit yet | ⬜ |
| 9 | BSC KPI **trajectory removed** (#5, PR #35; `kpi_target_trajectory` table dropped) | ✅ dev | BLO | Each tracked KPI had a Trajectory dropdown (Increase/Decrease/Maintain) + a Preview trend/Matched-Mismatched pill → both gone. **Targets** + the "Targets set" pill are unchanged | One-line note that trajectory is retired; nothing to configure | ⬜ |
| 10 | Calculator / formula builder — one builder for calculated inputs **and** KPIs; per-input tag-card bindings; Track-as-KPI facet; **role-tiered custom-KPI creation** (#3, spec [calculator-engine-spec.md](calculator-engine-spec.md) §5–§7.1 + `calculator-builder-mockup.html`; gated on #2 migration + manual KPI rebuild; **DEV builder CORE SHIPPED 2026-08-26 (PR #135/#138): unified 10-dim tag-card builder for KPIs + calculated measures, Compute-now, durable `formula_binding` tables — but DEV-gated, so the BMO/BLO journey below is NOT yet live**) | 🟡 partial (DEV core live; BMO/BLO surface + custom-KPI governance + per-slice output not built) | BMO, BLO | Two separate formula builders (inputs vs KPIs) with a 3-dimension JSON binding → **one** builder: write a formula, each variable gets a **tag card** (pick its measure + set each applicable dimension to **Pin / All / Inherit**, + a separate **Grain** control), and a **"Track as KPI"** toggle decides whether the computed measure is also published as a KPI. Measure picker browses by group/subgroup + type-ahead search; a deactivated/missing input measure shows an inline warning instead of silently dropping. **NEW for BLOs:** a "Create Custom KPI" mode over **existing measures only** (no create-measure — gap → **Request a measure** → BMO); a **Visibility** radio — **Private** (instant, own utility) vs **Share** (needs BMO approval before joining benchmarking) | BMO authoring guide (tag-card binding, same-measure-different-slice, Track-as-KPI vs plain input, measure picker, deactivated warning); **BLO custom-KPI guide** (catalogue-only picker, request-a-measure flow, Private-vs-Share and what approval means); BMO **share-approval / measure-request triage** guide | 🕐 |
| 11 | Measure **Description** field dropped from Inputs settings (#14, commit `4316624` + DB `measure_definitions.description` drop) | ✅ dev | BMO | Create/Edit Input (measure definition) form had a free-text **Description** textarea → removed. The separate **definition** field (with its draft/curated status) is unchanged and remains the place for a measure's meaning | One-line note that the legacy Description field is retired — use **definition** for measure meaning; nothing to configure | ⬜ |
| 12 | **"Is Aggregated Resource"** field dropped from Units settings (#14, commit `3ead895` + DB `units.is_aggregated` drop) | ✅ dev | BMO | Create/Edit Unit form (Units/energy-resources settings) had an **"Is Aggregated Resource"** checkbox → removed (unused; all rows were false) | One-line note that the flag is retired; nothing to configure | ⬜ |
| 13 | **external_registrations retired** — legacy free-text registration console removed + DB table dropped (#10, PR #79, `30f0f72`, 2026-07-28) | ✅ (+ dev DB) | BMO, org-admin, registrant | BMO had a separate `/settings/external-registrations` console listing free-text external sign-ups to accept/reject → that console is **gone**; external/non-utility sign-ups now flow through the **standard pending-user approval path** (`/settings/users`, `status=pending`) + the clarification thread. (The structured `access_request` intake — Row 5 — is the future replacement, still unbuilt.) | BMO guide note: approve external registrations via the **Pending Users** screen, not the old console; the old settings menu item is removed. No data migration (table was empty). | ⬜ |
| 14 | Energy-taxonomy vocab rename — managed lists → Strata/Provider/Category/Technology/Asset Class; item "Unit" (#4 code / #14 DB, `98cf4e0`/`b7b8473`) | ✅ dev | BMO | Managed-list screens showed old energy-dimension names → now show Strata/Provider/Category/Technology/Asset Class/Unit | Glossary note mapping old→new; BMO heads-up that taxonomy labels changed (display + list config) | ⬜ |
| 15 | 10 legacy managed lists deleted — Aggregation Group/Method, Data Group, DLSource/KPISource Tables, KPI Requester, Measure Type, Necessity, Product Level, Service Relevance Group (+60 items) (#2, direct DML 2026-08-03; backups `backup.managed_lists_del_20260803`/`managed_list_items_del_20260803`) | ✅ dev DB | BMO | Settings → Managed Lists (renders all lists unfiltered) showed these 10 as selectable entries → now 10 fewer. All were **vestigial** (0 live data, no referenced items) — clutter removed, not a workflow loss | None for end users. If any BMO help docs/screenshots reference these list names, update them | ⬜ |
| 16 | Country-context entry — metric field becomes a **select** + **Update/upsert** + a **no-data-reason option** (#11 build, follow-on to #4's country-context repoint `fcf8e4e` + `no_data_reason` axis `024d935`; `app/settings/country-context` is BMO-facing) | 🕐 not built | BMO | BMO enters/maintains national annual figures on the country-context screen; today the metric is free-form, insert-only, and value-only → will become a **select from measure catalogue** + an **Update/upsert** (edit existing, not just add) + the ability to mark a metric **"not available"** instead of entering a value | BMO country-context guide: pick the metric from the list, edit-vs-add behaviour, when/how to mark not-available; note it feeds AI/benchmarking national context | 🕐 |
| 17 | Fiscal-year-aware ReportPeriod labels — FY-end-aware derivation fixes calendar-year utilities (#4, `6504e7e`, code-only) | ✅ | BMO, analysts (Power BI) | Report-period labels assumed a uniform fiscal year → now derived from each org's `financial_year_end`; **~30 report periods relabel** for calendar-year utilities across all 24 fact routes' Power BI exports | Analyst note: FY labels for calendar-year utilities corrected — a period's label may differ from before; no action, just awareness | ⬜ |
| 18 | Data-entry validation goes live + financial sign/range conventions (#3, PR #150, DB `scripts/sql/2026-08-30-financial-validation-bounds.sql`) | ✅ dev (+ code) | DAO, BLO | The live v2 data-entry grid validated **nothing** → now validates on **entry (live, as you type: red message + border, invalid not saved)** and on **save (server, authoritative)** — type, range, sign. Financial convention: enter **positive magnitudes** (a negative cost is blocked: "must be greater than or equal to 0"); **Profit / Total Equity accept negatives** (loss/deficit); **currency amounts have no upper cap** (weak-currency amounts e.g. VUV/SBD no longer blocked). Empty stays valid (composes with the coming "not available" cell state) | Data-collector guide: enter amounts as positive; the inline red message + blocked save on invalid values; which fields accept negatives (Profit, Total Equity); the max-digit ceiling on currency amounts is gone | ⬜ |
| 19 | Publish-gating by approval status — only **Approved(5)** periods publish to Power BI / benchmarking (mechanism = #4's route gate `c9ae238` + status CHECK, already shipped; **trigger = #2's new-period-creation feature**, periods birthing at Pending — not yet built) | 🕐 not built (gate live, trigger pending) | BMO, analysts, utility CEO | Today all periods are Approved so all data publishes. Once #2's period-creation lands, **in-progress periods (Pending / Entered / Reviewed) are excluded from Power BI + benchmarking until the utility CEO approves the period** — only Approved(5) data publishes | CEO/BMO guide: what "approve the period" gates (nothing appears in Power BI/benchmarking until approval); analyst note that in-progress periods are intentionally absent, not missing data | 🕐 |
