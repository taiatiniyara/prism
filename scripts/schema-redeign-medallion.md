# MOVED — see `docs/schema-redesign-medallion.md`

This file was an early draft of the medallion schema redesign, created alongside the
implementation commits. It is **superseded** and no longer maintained.

The single source of truth is **`docs/schema-redesign-medallion.md`** (kept current with
every design decision), with two companion documents for the database builder:

- `docs/database-build-spec.md` — column-by-column configuration, keys, relationships,
  dependency order
- `docs/data-entries-configuration-guide.md` — phased execution guide with
  `scripts/configure-data-entries.sql`

(This stub is kept so old links don't dead-end; note the filename typo — "redeign".)
